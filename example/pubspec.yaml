name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_extended: ^3.3.0
  fl_wx:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

fluwx:
  app_id: 'test'
  debug_logging: true
  ios:
    universal_link: https://test/app/
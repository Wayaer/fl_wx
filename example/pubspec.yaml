name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_wx:
    path: ../
  fl_extended: ^1.5.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

fluwx:
  app_id: 'test'
  debug_logging: true
  ios:
    universal_link: https://test/app/